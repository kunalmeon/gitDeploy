header.header
  nav.nav.nav--tours
    a.nav__el(href='/api/tour') All tours
 
  .header__logo
    img(src='/img/logo-white.png' alt='Natours logo')
  nav.nav.nav--user
    //- 3a) So once the user data is passed from authcontroller as res.locals.user we can now check for
     it and access it in order to display user navigation(logout,profile,etc)
    if user  
      a.nav.logOut Log Out
      //- 9 a) Ok now we the user clicks in his photo we have to activate '/me' route.
      a.nav(href='/me')
        img(src=`/img/users/${user.photo}`)
        //Let us display user's first name only
        span=user.name.split(' ')[0]
    //- 3b) Once we have succefully show user navigation it now time to render home page.
        This logic is again implemented in login.js file.
    else    
      a.user-access(href='/login') Log in
      a.user-access(href='#') Sign up
  